services:
  - type: web
    name: comem-mon-cours
    runtime: static
    buildCommand: npm ci && node scripts/build-all.mjs --force
    staticPublishPath: ./dist
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_OPTIONS
        value: --max-old-space-size=2048
